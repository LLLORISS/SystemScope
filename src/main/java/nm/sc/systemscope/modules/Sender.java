package nm.sc.systemscope.modules;

/**
 * An enum representing the sender of a chat message.
 * The sender can be either a user or an assistant.
 */
public enum Sender {
    /**
     * Represents a message sent by the user.
     */
    user,

    /**
     * Represents a message sent by the assistant.
     */
    assistant,
}
